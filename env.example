# Supabase (required for auth and API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Google Maps (required for geocoding in scanpoints)
GOOGLE_MAPS_URL=https://maps.googleapis.com
GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# n8n scanpoint webhook (required for scanpoints/run API)
N8N_SCANPOINT_WEBHOOK_URL=https://growetic.app.n8n.cloud/webhook/ad61bcba-e45e-4ccf-80af-2c590874913c

# Optional: n8n webhook secret for x-webhook-secret header
# Add an IF node in n8n to reject requests without this header
N8N_WEBHOOK_SECRET=some-long-random-string

# n8n business enrichment webhook (for POST /api/businesses/enrich)
N8N_BUSINESS_ENRICH_WEBHOOK_URL=
